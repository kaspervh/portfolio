<%= form_with(model: work_exp, local: true) do |form| %>
  <% if work_exp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(work_exp.errors.count, "error") %> prohibited this work_exp from being saved:</h2>

      <ul>
      <% work_exp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :year %>
    <%= form.date_select :year, id: :work_exp_year %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :work_exp_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :work_exp_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
